{
  "name": "gbti-payment-gateway-backend",
  "version": "1.0.0",
  "description": "Mock GBTI Bank payment gateway — sandbox only. Never handles real PAN storage.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "uuid": "^11.0.5"
  }
}
