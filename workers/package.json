{
  "name": "gbti-payment-gateway-worker",
  "version": "1.0.0",
  "private": true,
  "description": "GBTI Bank sandbox payment gateway — Cloudflare Worker (Hono). Never stores PAN/CVV.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "wrangler": "4.119.0"
  },
  "engines": {
    "node": ">=18"
  }
}
